# Restream API Configuration
RESTREAM_CLIENT_ID=your_client_id_here
RESTREAM_CLIENT_SECRET=your_client_secret_here

# Blackbox AI — two separate keys for two APIs
# Chat completions (sk-xxxx) — get at https://www.blackbox.ai/dashboard → API Keys
BLACKBOX_CHAT_KEY=
# Multi-agent tasks (bb_xxxx) — get at https://cloud.blackbox.ai → API Keys
BLACKBOX_API_KEY=